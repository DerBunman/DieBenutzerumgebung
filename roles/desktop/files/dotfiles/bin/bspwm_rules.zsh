#!/usr/bin/env zsh
# =====================================
# << THIS FILE IS MANAGED BY ANSIBLE >>
# =====================================
#
# DO NOT EDIT THIS FILE - CHANGES WILL BE OVERWRITTEN
# INSTEAD CHANGE THIS FILE IN THE ANSIBLE REPOSITORY.

wid=$1
class=$2
instance=$3

rules=""

if [ "$instance" = fontforge ] ; then
	title=$(xdotool getwindowname "$wid")
	case "$title" in
		Layers|Tools|Warning)
			rules="focus=off ${rules}"
			;;
	esac

elif [ "$class" = "copyq" ]; then
	rules="state=floating ${rules}"

elif [ "$class" = "mpv" ]; then
	rules="state=floating ${rules}"

elif [ "$instance" = "pavucontrol" ]; then
	rules="state=floating ${rules}"

elif [ "$class" = "Steam" ]; then
	rules="desktop=^3 ${rules}"

elif [ "$class" = "usbguard-applet-qt" ]; then
	rules="state=floating ${rules}"

elif [ "$class" = "scratchpad_gvim" ]; then
	rules="sticky=on state=floating hidden=on ${rules}"
fi

echo "${rules}"
