#!/usr/bin/env zsh
if [[ -z "${1}" ]]; then
	echo "needs an command to wrap."
	echo "eg: ${0} indicator-cpufreq"
fi
if [ "${KILL_CMD}" ]; then
	zsh -c "${KILL_CMD}"
else
	pkill "${1:t}"
fi
#sleep 1
"$@" >/dev/null 2>&1 &!
